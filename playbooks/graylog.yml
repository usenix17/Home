---
# Equivalent to syslog.yaml -- both apply the syslog role (forward to Graylog).
# Kept for backward compat; consider consolidating to one.
- name: Configure rsyslog to forward logs to Graylog
  hosts: all
  become: true
  roles:
    - syslog
