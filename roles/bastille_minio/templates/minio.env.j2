# {{ ansible_managed }}
# MinIO root credentials. Sourced by the rc.d script. Mode must be 0600.
# Do not commit real values -- set minio_root_password via ansible-vault.
MINIO_ROOT_USER="{{ minio_root_user }}"
MINIO_ROOT_PASSWORD="{{ minio_root_password }}"
